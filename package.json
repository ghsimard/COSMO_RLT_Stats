{
  "name": "cosmo-stats",
  "version": "1.0.0",
  "description": "COSMO Stats Application",
  "main": "backend/dist/server.js",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "@types/pg": "^8.15.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "pg": "^8.15.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
